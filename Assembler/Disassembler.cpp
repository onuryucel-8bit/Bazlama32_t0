#include "Disassembler.h"

namespace asmc
{
	Dissambler::Dissambler()
	{
	}

	Dissambler::~Dissambler()
	{
	}

	void Dissambler::run(std::vector<asmc::MemoryLayout>& memlay)
	{
		for (size_t i = 0; i < memlay.size(); i++)
		{

		}
	}
}